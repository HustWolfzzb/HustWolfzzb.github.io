# Introduction

Sandrial starter


# Commands

**With windows, make sure VPN is alive.**
    gem install jekyll
    gem install bundler
    bundle install
    bundle update
    bundle exec jekyll serve